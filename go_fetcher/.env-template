DJANGO_URL=http://backend:8000

FETCHER_API_KEY=test-fetcher-key

PARSER_HEALTH_MARKETPLACE_TIMEOUT_SECONDS=120
PARSER_HEALTH_HANDLER_TIMEOUT_SECONDS=250
VPN_PARSE_DELAY_SECONDS=600  # prod = 600

WB_REQUEST_DELAY_MS=700
WB_BROWSER_FETCHER_URL=http://vpn_controller:8097
WB_BROWSER_FETCHER_ENABLED=true
WB_BROWSER_FETCHER_REQUIRED=true
WB_BROWSER_FETCHER_TIMEOUT_SECONDS=420
WB_MAX_RETRIES=3
WB_RETRY_BASE_DELAY_MS=1000

OZON_REQUEST_DELAY_MS=700
OZON_MAX_RETRIES=3
OZON_RETRY_BASE_DELAY_MS=1000
OZON_HTTP_PARSER_TIMEOUT_SECONDS=12

OZON_BROWSER_FETCHER_URL=http://vpn_controller:8097
OZON_BROWSER_FETCHER_ENABLED=true
OZON_BROWSER_FETCHER_REQUIRED=true
OZON_BROWSER_FETCHER_TIMEOUT_SECONDS=420

# Both browser workers launch Google Chrome Stable as a separate process,
# expose a local Chrome DevTools Protocol endpoint, and connect Playwright to it.
# Port 9222 exists only inside each browser container and is not published.
BROWSER_EXECUTABLE=/usr/bin/google-chrome-stable
BROWSER_CDP_HOST=127.0.0.1
BROWSER_CDP_PORT=9222
BROWSER_DISPLAY=:99
BROWSER_WINDOW_SIZE=1400,900
BROWSER_LANGUAGE=ru-RU
TZ=Europe/Moscow
BROWSER_START_TIMEOUT_SECONDS=30
BROWSER_RUNTIME_DIR=/tmp/browser-runtime
BROWSER_PROFILE_DIR=/data/browser-profile
BROWSER_PROFILE_RETENTION=20
BROWSER_RUNTIME_LOG_RETENTION=20
BROWSER_DISABLE_SANDBOX=true
BROWSER_DISABLE_DEV_SHM_USAGE=false
BROWSER_HEADLESS=false
BROWSER_REQUIRE_PROXY=true

# The proxy is supplied dynamically by vpn_controller for every VPN session.
# Keep BROWSER_PROXY_SERVER empty in production so a missing controller proxy
# fails closed instead of allowing a direct marketplace connection.
BROWSER_PROXY_SERVER=
BROWSER_PROXY_BYPASS_LIST=localhost;127.0.0.1
BROWSER_EXTRA_ARGS=

# Leave these URLs empty in production. When empty, each worker owns and starts
# its local Google Chrome process. An external CDP URL cannot safely accept the
# dynamic VPN proxy and is rejected when a proxy is supplied.
OZON_BROWSER_CDP_URL=
OZON_BROWSER_CDP_CONNECT_TIMEOUT_MS=10000
OZON_BROWSER_CDP_START_TIMEOUT_SECONDS=25
OZON_BROWSER_CDP_RETRY_DELAY_SECONDS=1
OZON_BROWSER_ACTION_TIMEOUT_MS=8000
OZON_BROWSER_NAVIGATION_TIMEOUT_MS=45000
OZON_BROWSER_CHALLENGE_TIMEOUT_MS=25000
OZON_BROWSER_CHALLENGE_REFRESH_ATTEMPTS=1
OZON_BROWSER_BLOCK_HEAVY_RESOURCES=false
OZON_BROWSER_IMPORT_COOKIE_FILE=true
OZON_BROWSER_COOKIE_IMPORT_EXCLUDE_NAMES=abt_data,__Secure-ETC

WB_BROWSER_CDP_URL=
WB_BROWSER_CDP_CONNECT_TIMEOUT_MS=10000
WB_BROWSER_CDP_START_TIMEOUT_SECONDS=25
WB_BROWSER_CDP_RETRY_DELAY_SECONDS=1
WB_BROWSER_IMPORT_COOKIE_FILE=true
WB_BROWSER_REQUEST_TIMEOUT_MS=60000
WB_BROWSER_NETWORK_WAIT_SAFETY_MARGIN_MS=5000
WB_BROWSER_NETWORK_WAIT_MIN_MS=1000
WB_BROWSER_NETWORK_WAIT_MAX_MS=40000
